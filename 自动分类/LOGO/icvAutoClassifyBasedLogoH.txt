/// =============================================================================
/// <						基于LOGO的特征匹配细分类								>
/// =============================================================================
/// <LOGO作为图像中一种特殊的标记，其具有唯一性功能，然而实际扫描会导致同一LOGO特征差异极大	>
/// <这种差异性导致LOGO的特征匹配极不稳定，从而在该版本中并未用到LOGO特征匹配				>
/// <一种稳健的LOGO特征，应该具备抗干扰性、尺度不变性、旋转不变性等						>
/// <原有算法中的sift特征具备尺度和旋转不变性，但其抗干扰性并不好							>
/// <可以采用其他特征进行特征(SURF, HOG, LBP, LIOP, BOW，灰度共生矩阵等特征)匹配			>
/// =============================================================================

#ifndef _ICV_AUTO_CLASSIFY_LOGO_H_
#define _ICV_AUTO_CLASSIFY_LOGO_H_
#include "icvTemplateInfo.h"

/// =============================================================================
/// <基于LOGO的模板匹配算法，实际未用到！>
int icvAutoClassifyBasedLOGO(const Mat img, const vector<ImageTemplate> vecIT, vector<int>& vecIndex);

/// =============================================================================
#endif